---
import { quizBlueprint } from '../../scripts/service-recommendation.mjs';

interface Props {
  locale: 'id' | 'en';
  origin?: 'learn';
  seriesId?: string;
}
const { locale, origin, seriesId } = Astro.props;

const copy = locale === 'id'
  ? {
      kicker: 'Rekomendasi layanan',
      meta: '5 pertanyaan · 1–2 menit',
      initialTitle: 'Temukan titik awal yang paling berguna',
      initialBody: 'Jawab lima pertanyaan singkat untuk menemukan prioritas di SEO, website, visual strategy, atau social media.',
      start: 'Temukan solusi Anda',
      privacy: 'Jawaban diproses di browser dan tidak dikirim atau disimpan.',
      calculating: 'Menyusun roadmap Anda…',
      progress: 'Pertanyaan',
      back: 'Kembali',
      choose: 'Pilih jawaban yang paling mendekati kondisi saat ini.',
      resultLabel: 'Arah yang direkomendasikan',
      problemLabel: 'Masalah yang teridentifikasi',
      priorityLabel: 'Prioritas pertama',
      supportLabel: 'Layanan pendukung',
      noSupport: 'Mulai dari satu prioritas ini. Layanan tambahan belum perlu dimasukkan ke scope awal.',
      viewService: 'Lihat konteks layanan',
      consultationCta: 'Lanjutkan ke konsultasi gratis',
      consultationSupport: 'Konsultasi pertama gratis. Setelah kami memahami konteks dan kebutuhan Anda, kami akan menjelaskan scope yang disarankan beserta harga setiap bagian yang memang diperlukan.',
      reset: 'Coba skenario lain',
      questions: [
        {
          title: 'Apa hambatan bisnis digital yang paling terasa sekarang?',
          answers: [
            ['visibility', 'Pelanggan yang tepat belum menemukan kami', 'Visibility untuk demand bernilai belum konsisten.'],
            ['clarity', 'Penawaran sulit dipahami atau dibandingkan', 'Website belum menjelaskan penawaran dan langkah berikutnya dengan cukup jelas.'],
            ['recognition', 'Brand sulit dikenali atau terasa tidak konsisten', 'Pesan dan sistem visual belum membantu orang memahami dan mengingat bisnis.'],
            ['publishing', 'Konten berjalan reaktif dan tidak konsisten', 'Produksi dan pembaruan konten belum memiliki sistem yang dapat dipertahankan.']
          ]
        },
        {
          title: 'Bagaimana kondisi website saat ini?',
          answers: [
            ['fragile', 'Lambat, bermasalah, atau sulit dipelihara', 'Fondasi teknis membatasi usability, search, atau perubahan bisnis.'],
            ['unclear', 'Berfungsi, tetapi journey dan conversion belum jelas', 'User journey belum mengubah perhatian menjadi tindakan yang jelas.'],
            ['stable', 'Stabil dan sudah dipantau', 'Fondasi website cukup stabil untuk fokus pada optimasi berikutnya.'],
            ['unknown', 'Belum pernah diaudit dengan serius', 'Risiko teknis dan measurement belum cukup terlihat untuk menentukan prioritas.']
          ]
        },
        {
          title: 'Dari mana demand yang ingin diperkuat?',
          answers: [
            ['google', 'Google dan organic search', 'Demand utama berada pada pencarian organik bernilai.'],
            ['social', 'Social media dan jaringan profesional', 'Expertise bisnis perlu hadir secara konsisten di hadapan audiens yang relevan.'],
            ['referral', 'Referral, presentasi, dan percakapan sales', 'Materi bisnis perlu menjelaskan penawaran dengan lebih jelas dan konsisten.'],
            ['mixed', 'Belum yakin—channel masih bercampur', 'Channel acquisition belum memiliki prioritas dan hubungan yang jelas.']
          ]
        },
        {
          title: 'Dukungan seperti apa yang realistis bagi tim?',
          answers: [
            ['managed', 'Partner untuk menjalankan pekerjaan berkelanjutan', 'Tim membutuhkan ownership eksternal untuk menjaga ritme eksekusi.'],
            ['direction', 'Tim bisa eksekusi dengan arah dan sistem visual yang jelas', 'Tim membutuhkan panduan pesan dan visual untuk membuat materi secara konsisten.'],
            ['development', 'Tim siap berkolaborasi dengan technical delivery', 'Ada kesiapan internal untuk menjalankan perubahan teknis secara terstruktur.'],
            ['focused', 'Mulai dari scope kecil dan fokus', 'Scope awal perlu dibatasi pada intervensi dengan dampak paling langsung.']
          ]
        },
        {
          title: 'Outcome apa yang paling berarti dalam 3–6 bulan ke depan?',
          answers: [
            ['pipeline', 'Lebih banyak inquiry organik yang relevan', 'Peluang organic search perlu terhubung lebih jelas dengan pipeline bisnis.'],
            ['recognition', 'Brand lebih mudah dipahami dan dikenali', 'Pesan dan visual perlu membangun pemahaman serta pengenalan yang konsisten.'],
            ['conversion', 'Journey website yang lebih cepat, jelas, dan menghasilkan tindakan', 'Friction pada experience dan conversion perlu dikurangi dengan perubahan website yang terukur.'],
            ['cadence', 'Sistem konten yang dapat dijaga oleh tim', 'Bisnis membutuhkan ritme, ownership, dan standar konten yang dapat dipertahankan.']
          ]
        }
      ],
      services: {
        visual: { name:'Visual Strategy', slug:'visual-strategy', reason:'Pola jawaban menunjukkan bahwa pesan dan sistem visual perlu diperjelas agar bisnis lebih mudah dipahami, dikenali, dan diingat.', support:'Visual Strategy menyatukan cara pesan bisnis ditampilkan di setiap titik kontak.' },
        social: { name:'Social Media Management', slug:'social-media-management', reason:'Pola jawaban menunjukkan kebutuhan untuk mengubah expertise bisnis menjadi konten yang relevan dan kehadiran pasar yang konsisten.', support:'Social Media Management membawa expertise kepada audiens yang relevan dengan ritme yang dapat dijaga.' },
        seo: {
          name: 'SEO',
          slug: 'seo',
          reason: 'SEO menjadi prioritas karena pola jawaban menunjukkan gap antara demand bernilai, struktur halaman, dan kemampuan pelanggan menemukan penawaran Anda.',
          support: 'SEO menghubungkan halaman prioritas dengan demand yang benar-benar dicari pelanggan.'
        },

        web: {
          name: 'Web Services',
          slug: 'web-services',
          reason: 'Web Services menjadi prioritas karena fondasi teknis atau user journey saat ini menghambat pemahaman, measurement, dan conversion.',
          support: 'Web Services memastikan rekomendasi search dapat benar-benar diimplementasikan.'
        },

      }
    }
  : {
      kicker: 'Your Growth Roadmap',
      meta: '5 questions · 1–2 minutes',
      initialTitle: 'Find the first move worth making',
      initialBody: 'Answer five short questions to find your next priority in search, websites, visual strategy, or social media.',
      start: 'Find your solution',
      privacy: 'Answers run in your browser and are never sent or stored.',
      calculating: 'Creating your roadmap…',
      progress: 'Question',
      back: 'Back',
      choose: 'Choose the answer closest to your situation today.',
      resultLabel: 'Recommended direction',
      problemLabel: 'Problem identified',
      priorityLabel: 'First priority',
      supportLabel: 'Supporting service',
      noSupport: 'Start with this single priority. An additional service does not need to enter the initial scope yet.',
      viewService: 'View service context',
      consultationCta: 'Continue to a free consultation',
      consultationSupport: 'Your first consultation is free. Once we understand the context and what you actually need, we will explain the recommended scope and the price of each required piece.',
      reset: 'Try another scenario',
      questions: [
        {
          title: 'Which digital business constraint feels most urgent?',
          answers: [
            ['visibility', 'The right customers do not find us', 'Visibility around valuable demand is not yet consistent.'],
            ['clarity', 'Our offer is hard to understand or compare', 'The website does not explain the offer and next step clearly enough.'],
            ['recognition', 'Our brand is hard to recognise or feels inconsistent', 'The message and visual system do not help people understand and remember the business.'],
            ['publishing', 'Content is reactive and inconsistent', 'Content production and refreshes lack a sustainable operating system.']
          ]
        },
        {
          title: 'What is the website foundation like today?',
          answers: [
            ['fragile', 'Slow, fragile, or difficult to maintain', 'Technical foundations are limiting usability, search, or business change.'],
            ['unclear', 'It works, but the journey and conversion are unclear', 'The user journey does not turn attention into a clear action.'],
            ['stable', 'Stable and actively monitored', 'The website foundation is stable enough to focus on the next optimization.'],
            ['unknown', 'It has never been reviewed properly', 'Technical and measurement risk is not visible enough to prioritize confidently.']
          ]
        },
        {
          title: 'Where should stronger demand come from?',
          answers: [
            ['google', 'Google and organic search', 'The main opportunity sits in valuable organic search demand.'],
            ['social', 'Social media and professional networks', 'Business expertise needs a consistent presence in front of relevant audiences.'],
            ['referral', 'Referrals, presentations, and sales conversations', 'Business materials need to explain the offer clearly and consistently.'],
            ['mixed', 'We are unsure—the channels are mixed', 'Acquisition channels lack a clear priority and relationship.']
          ]
        },
        {
          title: 'What support can the team realistically sustain?',
          answers: [
            ['managed', 'A partner to run ongoing work', 'The team needs external ownership to maintain execution rhythm.'],
            ['direction', 'We can execute with clear direction and a visual system', 'The team needs message and visual guidance to produce consistent materials.'],
            ['development', 'We are ready to collaborate on technical delivery', 'The team is ready to implement technical change in a structured way.'],
            ['focused', 'Start with a small, focused scope', 'The first scope should stay focused on the intervention with the most direct impact.']
          ]
        },
        {
          title: 'Which outcome would matter most in the next 3–6 months?',
          answers: [
            ['pipeline', 'More relevant enquiries from organic search', 'The organic search opportunity needs a clearer connection to the business pipeline.'],
            ['recognition', 'A brand people understand and recognise', 'The message and visuals need to build clarity and consistent recognition.'],
            ['conversion', 'A faster, clearer website journey that drives action', 'Experience and conversion friction need measurable website changes.'],
            ['cadence', 'A content system the team can maintain', 'The business needs a sustainable content rhythm, ownership model, and quality standard.']
          ]
        }
      ],
      services: {
        visual: { name:'Visual Strategy', slug:'visual-strategy', reason:'Your answers point to a need for a clearer message and visual system, so the business is easier to understand, recognise, and remember.', support:'Visual Strategy connects how the business communicates across its touchpoints.' },
        social: { name:'Social Media Management', slug:'social-media-management', reason:'Your answers point to a need to turn business expertise into relevant content and a consistent market presence.', support:'Social Media Management brings expertise to relevant audiences at a sustainable rhythm.' },
        seo: {
          name: 'SEO',
          slug: 'seo',
          reason: 'SEO is the priority because the answer pattern points to a gap between valuable demand, page structure, and the ability of customers to find the offer.',
          support: 'SEO connects priority pages with the demand customers are actively searching for.'
        },

        web: {
          name: 'Web Services',
          slug: 'web-services',
          reason: 'Web Services is the priority because technical foundations or the user journey are limiting understanding, measurement, and conversion.',
          support: 'Web Services makes search recommendations practical to implement.'
        },

      }
    };

const questions = quizBlueprint.map((question, index) => ({
  ...copy.questions[index],
  id: question.id
}));
---

<section
  id="website-growth-roadmap"
  class="roadmap flow-section"
  aria-labelledby="quiz-title"
  data-service-quiz
  data-persistent-field
  data-load-state="pending"
  data-locale={locale}
  data-origin={origin}
  data-series-id={seriesId}
  data-quiz-copy={JSON.stringify({ progress: copy.progress, noSupport: copy.noSupport, services: copy.services })}
>
  <div class="container quiz-shell">
    <div class="quiz-frame">
      <div class="quiz-loader" data-quiz-loader aria-hidden="true">
        <span></span><span></span><span></span>
      </div>

      <section class="quiz-view quiz-intro" data-quiz-view="intro">
        <div class="quiz-intro__content">
          <span class="quiz-meta text-mono">{copy.meta}</span>
          <h2 id="quiz-title">{copy.initialTitle}</h2>
          <p>{copy.initialBody}</p>
          <button class="btn btn-primary" type="button" data-quiz-start>{copy.start}</button>
          <small>{copy.privacy}</small>
        </div>
      </section>

      <section class="quiz-view quiz-questions" data-quiz-view="questions" hidden>
        <header class="quiz-progress">
          <span class="text-mono" data-progress-label>{copy.progress} 1 / {questions.length}</span>
          <div class="progress-track" aria-hidden="true"><span data-progress-fill></span></div>
        </header>

        <div class="question-stage" data-question-stage>
          {questions.map((question, questionIndex) => (
            <fieldset class="question-panel" data-question={questionIndex} data-question-id={question.id} hidden={questionIndex !== 0}>
              <legend tabindex="-1">{question.title}</legend>
              <p class="question-hint">{copy.choose}</p>
              <div class="answer-grid">
                {question.answers.map(([answerId, label, problem]) => (
                  <button
                    type="button"
                    class="answer-option"
                    data-answer
                    data-answer-id={answerId}
                    data-problem={problem}
                    aria-pressed="false"
                  >
                    <span>{label}</span>
                  </button>
                ))}
              </div>
            </fieldset>
          ))}
        </div>

        <footer class="quiz-controls">
          <button type="button" class="quiz-back" data-quiz-back disabled>
            <span aria-hidden="true">←</span> {copy.back}
          </button>
        </footer>
      </section>

      <section class="quiz-view quiz-calculating" data-quiz-view="calculating" hidden aria-live="polite">
        <div class="recommendation-loader" aria-hidden="true">
          <span></span><span></span><span></span>
        </div>
        <p>{copy.calculating}</p>
      </section>

      <section class="quiz-view quiz-result" data-quiz-view="result" hidden aria-live="polite">
        <div class="result-primary">
          <span class="section-kicker">{copy.resultLabel}</span>
          <h2 data-result-title></h2>
          <div class="service-combination" data-service-combination aria-label={copy.resultLabel}></div>
          <a class="btn btn-primary btn-cta roadmap-consultation" data-consultation-link href={'/' + locale + '/contact/?source=roadmap'}>
            <span>{copy.consultationCta}</span>
          </a>
          <p class="consultation-support">{copy.consultationSupport}</p>
          <a class="link-hover-arrow" data-service-link href={'/' + locale + '/services/'}>
            <span>{copy.viewService}</span><span aria-hidden="true">→</span>
          </a>
        </div>

        <div class="result-explanation">
          <div class="result-block">
            <span class="text-mono">{copy.problemLabel}</span>
            <ul data-result-problems></ul>
          </div>
          <div class="result-block">
            <span class="text-mono">{copy.priorityLabel}</span>
            <p data-priority-reason></p>
          </div>
          <div class="result-block result-support">
            <span class="text-mono">{copy.supportLabel}</span>
            <p data-support-reason></p>
          </div>
          <button type="button" class="quiz-reset" data-quiz-reset>{copy.reset}</button>
        </div>
      </section>
    </div>
  </div>
</section>

<script>
  import { recommendServices } from '../../scripts/service-recommendation.mjs';
  import { trackFunnelEvent } from '../../scripts/funnel-analytics';

  const setupServiceQuiz = () => {
    document.querySelectorAll<HTMLElement>('[data-service-quiz]').forEach((root) => {
      if (root.dataset.ready === 'true') {
        if (root.dataset.loadState === 'pending') root.dataset.loadState = 'ready';
        return;
      }
      root.dataset.ready = 'true';

      const incomingParams = new URLSearchParams(window.location.search);
      const learnContext = {
        origin: root.dataset.origin ?? (incomingParams.get('source') === 'learn' ? 'learn' : ''),
        series_id: root.dataset.seriesId ?? incomingParams.get('series') ?? '',
        lesson_id: incomingParams.get('lesson') ?? '',
        resource_id: incomingParams.get('resource') ?? '',
      };
      const withLearnContext = (detail: Record<string, string> = {}) => ({
        ...detail,
        ...Object.fromEntries(Object.entries(learnContext).filter(([, value]) => value.length > 0)),
      });

      if (learnContext.origin === 'learn') {
        trackFunnelEvent('learn_roadmap_view', withLearnContext({ locale: root.dataset.locale ?? '' }));
      }

      const quizCopy = JSON.parse(root.dataset.quizCopy ?? '{}');
      const views = {
        intro: root.querySelector<HTMLElement>('[data-quiz-view="intro"]'),
        questions: root.querySelector<HTMLElement>('[data-quiz-view="questions"]'),
        calculating: root.querySelector<HTMLElement>('[data-quiz-view="calculating"]'),
        result: root.querySelector<HTMLElement>('[data-quiz-view="result"]')
      };
      const panels = Array.from(root.querySelectorAll<HTMLElement>('[data-question]'));
      const stage = root.querySelector<HTMLElement>('[data-question-stage]');
      const progressLabel = root.querySelector<HTMLElement>('[data-progress-label]');
      const progressFill = root.querySelector<HTMLElement>('[data-progress-fill]');
      const backButton = root.querySelector<HTMLButtonElement>('[data-quiz-back]');
      const answers: Record<string, string> = {};
      const reducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
      let currentQuestion = 0;
      let moving = false;

      const exitDuration = reducedMotion ? 0 : 220;
      const enterDuration = reducedMotion ? 0 : 340;

      const settleTarget = (target: HTMLElement) => {
        requestAnimationFrame(() => {
          requestAnimationFrame(() => target.classList.remove('is-entering'));
        });
        window.setTimeout(() => {
          moving = false;
        }, enterDuration);
      };

      const transitionView = (target: HTMLElement | null, update?: () => void) => {
        if (!target || moving) return;
        moving = true;
        const active = root.querySelector<HTMLElement>('.quiz-view:not([hidden])');
        active?.classList.add('is-leaving');

        window.setTimeout(() => {
          if (active) {
            active.hidden = true;
            active.classList.remove('is-leaving');
          }
          update?.();
          target.hidden = false;
          target.classList.add('is-entering');
          settleTarget(target);
        }, exitDuration);
      };

      const renderQuestion = (index: number, focus = true) => {
        currentQuestion = index;
        panels.forEach((panel, panelIndex) => {
          panel.hidden = panelIndex !== index;
        });
        if (progressLabel) progressLabel.textContent = quizCopy.progress + ' ' + (index + 1) + ' / ' + panels.length;
        if (progressFill) progressFill.style.width = ((index + 1) / panels.length * 100) + '%';
        if (backButton) backButton.disabled = index === 0;
        if (focus) panels[index]?.querySelector<HTMLElement>('legend')?.focus();
      };

      const transitionQuestion = (nextIndex: number) => {
        if (!stage || moving) return;
        moving = true;
        stage.classList.add('is-leaving');
        window.setTimeout(() => {
          renderQuestion(nextIndex, false);
          stage.classList.remove('is-leaving');
          stage.classList.add('is-entering');
          panels[nextIndex]?.querySelector<HTMLElement>('legend')?.focus();
          settleTarget(stage);
        }, exitDuration);
      };

      const revealQuiz = () => {
        if (root.dataset.loadState === 'ready') return;
        const revealDelay = reducedMotion ? 0 : 420;
        window.setTimeout(() => {
          root.dataset.loadState = 'ready';
        }, revealDelay);
      };

      if (reducedMotion || !('IntersectionObserver' in window)) {
        revealQuiz();
      } else {
        const revealObserver = new IntersectionObserver((entries) => {
          entries.forEach((entry) => {
            if (!entry.isIntersecting) return;
            revealQuiz();
            revealObserver.disconnect();
          });
        }, { rootMargin: '0px 0px -8% 0px', threshold: 0.12 });
        revealObserver.observe(root);
      }

      const showResult = () => {
        const result = recommendServices(answers);
        const services = quizCopy.services as Record<string, { name: string; slug: string; reason: string; support: string }>;
        const primary = services[result.primary];
        const supportKeys = result.supporting;
        const selectedProblems = panels
          .map((panel) => panel.querySelector<HTMLButtonElement>('[data-answer][aria-pressed="true"]')?.dataset.problem)
          .filter((problem): problem is string => Boolean(problem))
          .slice(0, 3);

        const resultTitle = root.querySelector<HTMLElement>('[data-result-title]');
        const combination = root.querySelector<HTMLElement>('[data-service-combination]');
        const problems = root.querySelector<HTMLUListElement>('[data-result-problems]');
        const priorityReason = root.querySelector<HTMLElement>('[data-priority-reason]');
        const supportReason = root.querySelector<HTMLElement>('[data-support-reason]');
        const link = root.querySelector<HTMLAnchorElement>('[data-service-link]');
        const consultationLink = root.querySelector<HTMLAnchorElement>('[data-consultation-link]');

        if (resultTitle) resultTitle.textContent = primary.name;
        if (priorityReason) priorityReason.textContent = primary.reason;
        if (supportReason) {
          supportReason.textContent = supportKeys.length
            ? supportKeys.map((key) => services[key].support).join(' ')
            : quizCopy.noSupport;
        }
        if (link) link.href = '/' + root.dataset.locale + '/services/' + primary.slug + '/';
        if (consultationLink) {
          const consultationParams = new URLSearchParams({ source: 'roadmap', service: primary.slug });
          Object.entries(learnContext).forEach(([key, value]) => {
            if (value) consultationParams.set(key, value);
          });
          consultationLink.href = '/' + root.dataset.locale + '/contact/?' + consultationParams.toString();
          consultationLink.dataset.recommendedService = primary.slug;
        }

        if (combination) {
          combination.replaceChildren();
          [result.primary, ...supportKeys].forEach((key, index) => {
            const tag = document.createElement('span');
            tag.textContent = services[key].name;
            tag.dataset.priority = index === 0 ? 'primary' : 'support';
            combination.append(tag);
          });
        }

        if (problems) {
          problems.replaceChildren();
          selectedProblems.forEach((problem) => {
            const item = document.createElement('li');
            item.textContent = problem;
            problems.append(item);
          });
        }

        trackFunnelEvent('roadmap_complete', withLearnContext({ locale: root.dataset.locale ?? '' }));
        trackFunnelEvent('roadmap_result', withLearnContext({ recommended_service: primary.slug }));

        transitionView(views.calculating, () => {
          root.dataset.quizState = 'calculating';
          window.setTimeout(() => {
            transitionView(views.result, () => {
              root.dataset.quizState = 'result';
            });
          }, 2000);
        });
      };

      root.querySelector<HTMLButtonElement>('[data-quiz-start]')?.addEventListener('click', () => {
        trackFunnelEvent('roadmap_start', withLearnContext({ locale: root.dataset.locale ?? '' }));
        transitionView(views.questions, () => {
          root.dataset.quizState = 'questions';
          renderQuestion(0, false);
        });
      });

      panels.forEach((panel, panelIndex) => {
        panel.querySelectorAll<HTMLButtonElement>('[data-answer]').forEach((button) => {
          button.addEventListener('click', () => {
            if (moving) return;
            const questionId = panel.dataset.questionId;
            const answerId = button.dataset.answerId;
            if (!questionId || !answerId) return;

            answers[questionId] = answerId;
            panel.querySelectorAll('[data-answer]').forEach((option) => {
              option.setAttribute('aria-pressed', String(option === button));
            });

            if (panelIndex === panels.length - 1) showResult();
            else transitionQuestion(panelIndex + 1);
          });
        });
      });

      backButton?.addEventListener('click', () => {
        if (currentQuestion > 0) transitionQuestion(currentQuestion - 1);
      });

      root.querySelector<HTMLAnchorElement>('[data-consultation-link]')?.addEventListener('click', (event) => {
        const link = event.currentTarget as HTMLAnchorElement;
        trackFunnelEvent('roadmap_consultation_click', withLearnContext({
          recommended_service: link.dataset.recommendedService ?? ''
        }));
        if (learnContext.origin === 'learn') {
          trackFunnelEvent('learn_consultation_click', withLearnContext({ recommended_service: link.dataset.recommendedService ?? '' }));
        }
      });

      root.querySelector<HTMLButtonElement>('[data-quiz-reset]')?.addEventListener('click', () => {
        Object.keys(answers).forEach((key) => delete answers[key]);
        root.querySelectorAll('[data-answer]').forEach((button) => button.setAttribute('aria-pressed', 'false'));
        transitionView(views.intro, () => {
          root.dataset.quizState = 'intro';
          renderQuestion(0, false);
        });
      });
    });
  };

  setupServiceQuiz();
  document.addEventListener('astro:page-load', setupServiceQuiz);
</script>

<style>
  .roadmap {
    height: 100svh;
    min-height: 640px;
    display: grid;
    place-items: center;
    position: relative;
    isolation: isolate;
    overflow: hidden;
    margin-top: -2px;
    padding: clamp(38px, 5vh, 56px) 0 8px;
    background: transparent;
  }

  .roadmap::before {
    content: '';
    position: absolute;
    inset: 0;
    z-index: 0;
    pointer-events: none;
    opacity: 1;
    background:radial-gradient(ellipse 70% 35% at 35% 45%,#f0f7fa,#cfe5f1 48%,transparent 100%),
      linear-gradient(180deg,transparent,#9ebed5 22%,#e3f0f7 42%,#b8d4e7 72%,transparent);
    mask-image:linear-gradient(180deg,transparent,#000 15%,#000 82%,transparent);
    transition: opacity 520ms cubic-bezier(0.16, 1, 0.3, 1);
  }


  .quiz-shell {
    position: relative;
    z-index: 1;
    height: 100%;
    min-height: 0;
  }

  .quiz-frame {
    position: relative;
    height: 100%;
    min-height: 0;
    display: grid;
    overflow: hidden;
    background: transparent;
  }

  .quiz-loader {
    position: absolute;
    inset: 50% auto auto 50%;
    z-index: 4;
    display: flex;
    gap: 8px;
    transform: translate(-50%, -50%);
    transition: opacity 300ms ease, visibility 300ms ease;
  }
  .quiz-loader span { width: 7px; aspect-ratio: 1; border-radius: 50%; background: var(--signal-ice); animation: quiz-loader-pulse 760ms ease-in-out infinite alternate; }
  .quiz-loader span:nth-child(2) { animation-delay: 110ms; }
  .quiz-loader span:nth-child(3) { animation-delay: 220ms; }
  [data-service-quiz][data-load-state='pending'] .quiz-view { opacity: 0; transform: translate3d(0, 12px, 0); }
  [data-service-quiz][data-load-state='ready'] .quiz-loader { opacity: 0; visibility: hidden; }
  [data-service-quiz][data-load-state='ready'] .quiz-view { opacity: 1; transform: none; }

  @keyframes quiz-loader-pulse {
    from { opacity: 0.24; transform: translate3d(0, 4px, 0) scale(0.82); }
    to { opacity: 1; transform: translate3d(0, -4px, 0) scale(1); }
  }

  .quiz-meta { display: block; color: rgba(201, 211, 223, 0.58); font-size: 0.68rem; }
  .quiz-view { min-height: 0; transition: opacity 340ms cubic-bezier(0.16, 1, 0.3, 1), transform 420ms cubic-bezier(0.16, 1, 0.3, 1), filter 320ms ease; }
  .quiz-view[hidden] { display: none; }

  .quiz-intro {
    display: grid;
    place-items: center;
    border-radius: 0;
    padding: clamp(38px, 7vh, 76px) clamp(24px, 6vw, 84px);
    background: transparent;
    color: #081522;
    text-align: center;
  }

  .quiz-intro__content { width: min(100%, 860px); display: grid; justify-items: center; }
  .quiz-intro .quiz-meta { margin-bottom: 24px; color: #3a5364; }

  .quiz-intro h2 {
    max-width: 14ch;
    color: #081522;
    font-size: clamp(3rem, 5.3vw, 6rem);
    font-weight: 520;
    letter-spacing: -0.04em;
    line-height: 0.93;
  }

  .quiz-intro p { max-width: 54ch; margin-top: 24px; color: #263d4c; font-size: clamp(1rem, 1.2vw, 1.14rem); }
  .quiz-intro .btn {
    margin-top: 30px;
    border: 0;
    background: #0b2234;
    color: var(--cloud-white);
  }
  .quiz-intro small { max-width: 46ch; margin-top: 16px; color: #435b6b; font-size: 0.74rem; line-height: 1.5; }

  @media (hover: hover) and (pointer: fine) {
    .quiz-intro .btn:hover {
      background: #123a54;
      transform: translateY(-2px);
    }
  }

  .quiz-questions {
    display: grid;
    grid-template-rows: auto minmax(0, 1fr) auto;
    border-radius: 12px;
    padding: clamp(24px, 4vh, 38px) clamp(24px, 6vw, 84px) 24px;
    background: transparent;
  }

  .quiz-progress { width: min(100%, 760px); display: grid; grid-template-columns: auto minmax(120px, 320px); gap: 24px; align-items: center; justify-content: center; margin-inline: auto; }
  .quiz-progress > span { color: rgba(244, 247, 251, 0.76); font-size: 0.7rem; }
  .progress-track { height: 2px; overflow: hidden; background: rgba(244, 247, 251, 0.24); }
  .progress-track span { display: block; width: 20%; height: 100%; background: var(--signal-ice); transition: width 360ms cubic-bezier(0.16, 1, 0.3, 1); }

  .question-stage { min-height: 0; display: grid; align-items: center; transition: opacity 320ms cubic-bezier(0.16, 1, 0.3, 1), transform 380ms cubic-bezier(0.16, 1, 0.3, 1), filter 300ms ease; }
  .question-panel { width: 100%; min-width: 0; border: 0; text-align: center; }
  .question-panel[hidden] { display: none; }
  .question-panel legend {
    max-width: 22ch;
    margin-inline: auto;
    color: var(--cloud-white);
    font-size: clamp(2rem, 3.5vw, 4rem);
    font-weight: 520;
    letter-spacing: -0.04em;
    line-height: 1;
    text-wrap: balance;
  }
  .question-panel legend:focus { outline: none; }
  .question-hint { margin: 14px auto 0; color: rgba(244, 247, 251, 0.82); font-size: 0.82rem; }
  .answer-grid { width: min(100%, 920px); display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px; margin: clamp(24px, 4vh, 38px) auto 0; }

  .answer-option {
    min-height: 76px;
    display: grid;
    place-items: center;
    border: 0;
    border-radius: 8px;
    padding: 15px 18px;
    background: rgba(17, 39, 57, 0.58);
    color: rgba(238, 245, 250, 0.86);
    text-align: center;
    transition: opacity 220ms ease, transform 300ms cubic-bezier(0.16, 1, 0.3, 1), background-color 260ms ease, color 260ms ease;
  }

  .answer-option[aria-pressed='true'] { background: rgba(103, 217, 255, 0.16); color: var(--cloud-white); transform: scale(0.985); }

  .quiz-controls { display: flex; justify-content: space-between; padding-top: 10px; }
  .quiz-back { min-height: 38px; color: rgba(244, 247, 251, 0.82); font-size: 0.82rem; }
  .quiz-back:disabled { opacity: 0; pointer-events: none; }

  .quiz-result {
    display: grid;
    grid-template-columns: minmax(270px, 0.78fr) minmax(0, 1.22fr);
    gap: clamp(42px, 8vw, 116px);
    align-items: center;
    padding: clamp(28px, 5vh, 54px) clamp(24px, 6vw, 84px);
  }

  .result-primary h2 { max-width: 12ch; margin: 22px 0; font-size: clamp(2.8rem, 5vw, 5.8rem); font-weight: 520; letter-spacing: -0.048em; line-height: 0.92; }
  .service-combination { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 24px; }
  .service-combination span { border: 1px solid rgba(201, 211, 223, 0.22); border-radius: 999px; padding: 7px 11px; color: rgba(226, 234, 243, 0.72); font-size: 0.72rem; }
  .service-combination span[data-priority='primary'] { border-color: rgba(207, 247, 255, 0.52); background: rgba(103, 217, 255, 0.1); color: var(--signal-ice); }
  .roadmap-consultation { width: fit-content; max-width: 100%; margin-bottom: 14px; }
  .consultation-support { max-width: 48ch; margin-bottom: 18px; color: rgba(201, 211, 223, 0.72); font-size: clamp(0.78rem, 0.72rem + 0.18vw, 0.9rem); line-height: 1.55; }

  .result-explanation { min-width: 0; display: grid; gap: clamp(15px, 2.4vh, 24px); }
  .result-block { padding-top: 16px; border-top: 1px solid rgba(89, 106, 130, 0.32); }
  .result-block > .text-mono { display: block; margin-bottom: 9px; color: rgba(201, 211, 223, 0.5); font-size: 0.65rem; }
  .result-block p,
  .result-block li { color: rgba(226, 234, 243, 0.78); font-size: clamp(0.82rem, 1vw, 0.96rem); line-height: 1.52; }
  .result-block ul { display: grid; gap: 5px; padding-left: 18px; }
  .quiz-reset { width: fit-content; justify-self: start; min-height: 38px; margin-top: 4px; color: rgba(201, 211, 223, 0.68); font-size: 0.82rem; text-decoration: underline; text-decoration-color: rgba(201, 211, 223, 0.28); text-underline-offset: 5px; }

  .is-leaving { opacity: 0; transform: translate3d(0, -10px, 0); filter: blur(4px); }
  .is-entering { opacity: 0; transform: translate3d(0, 10px, 0); filter: blur(4px); }

  @media (hover: hover) and (pointer: fine) {
    .answer-option:hover { background: rgba(103, 217, 255, 0.12); transform: translate3d(0, -3px, 0); }
    .quiz-back:not(:disabled):hover { color: var(--cloud-white); }
  }

  @media (max-width: 760px) {
    .roadmap { min-height: 100svh; padding: 75px 0 6px; }
    .quiz-shell { height: 100%; min-height: 0; padding-inline: 10px; }
    .quiz-intro { align-content: center; padding: 28px 16px; }
    .quiz-intro .quiz-meta { margin-bottom: 18px; }
    .quiz-intro h2 { font-size: clamp(2.45rem, 12vw, 4rem); }
    .quiz-questions { padding: 20px 18px 16px; }
    .quiz-progress { grid-template-columns: auto minmax(80px, 1fr); }
    .question-panel legend { max-width: 19ch; font-size: clamp(1.8rem, 8vw, 2.7rem); }
    .answer-grid { grid-template-columns: 1fr; gap: 8px; margin-top: 22px; }
    .answer-option { min-height: 58px; padding: 11px 13px; font-size: 0.88rem; }
    .quiz-result { grid-template-columns: 1fr; gap: 20px; align-content: start; padding: 22px 20px; overflow-y: auto; }
    .result-primary h2 { margin: 14px 0; font-size: clamp(2.5rem, 12vw, 4rem); }
    .service-combination { margin-bottom: 14px; }
    .consultation-support { margin-bottom: 13px; }
    .result-explanation { gap: 12px; }
    .result-block { padding-top: 11px; }
    .result-block:nth-child(1) li:nth-child(3) { display: none; }
  }

  @media (max-height: 720px) and (min-width: 761px) {
    .quiz-shell { min-height: 580px; }
    .quiz-intro h2 { font-size: clamp(2.8rem, 5vw, 4.8rem); }
    .question-panel legend { font-size: clamp(1.9rem, 3.2vw, 3.2rem); }
    .answer-option { min-height: 64px; }
    .quiz-result { gap: 56px; padding-block: 24px; }
    .result-primary h2 { font-size: clamp(2.6rem, 4.4vw, 4.4rem); }
    .result-explanation { gap: 12px; }
  }

  @media (prefers-reduced-motion: reduce) {
    .quiz-loader span { animation: none; }
    .quiz-loader { display: none; }
    .question-stage,
    .progress-track span,
    .answer-option,
    .quiz-view { transition: none; }
    .is-leaving,
    .is-entering { opacity: 1; transform: none; filter: none; }
  }
  [data-service-quiz][data-load-state='pending'] .quiz-view { pointer-events: none; }
  [data-service-quiz][data-load-state='ready'] .quiz-view { pointer-events: auto; }

  .quiz-calculating {
    display: grid;
    place-content: center;
    justify-items: center;
    gap: 20px;
    padding: clamp(28px, 6vh, 64px) 24px;
    text-align: center;
  }

  .quiz-calculating p {
    max-width: none;
    color: rgba(226, 234, 243, 0.76);
    font-size: 0.86rem;
  }

  .recommendation-loader {
    display: flex;
    align-items: center;
    gap: 9px;
    min-height: 44px;
  }

  .recommendation-loader span {
    width: 9px;
    aspect-ratio: 1;
    border-radius: 50%;
    background: var(--signal-ice);
    box-shadow: 0 0 18px rgba(103, 217, 255, 0.38);
    animation: quiz-loader-pulse 760ms ease-in-out infinite alternate;
  }

  .recommendation-loader span:nth-child(2) { animation-delay: 110ms; }
  .recommendation-loader span:nth-child(3) { animation-delay: 220ms; }

  @media (prefers-reduced-motion: reduce) {
    .recommendation-loader span { animation: none; }
  }
  .recommendation-loader {
    position: relative;
    width: 156px;
    justify-content: space-between;
    gap: 0;
  }

  .recommendation-loader::before,
  .recommendation-loader::after {
    content: '';
    position: absolute;
    inset: 50% 4px auto;
    height: 1px;
    transform: translateY(-50%);
  }

  .recommendation-loader::before { background: rgba(201, 211, 223, 0.18); }

  .recommendation-loader::after {
    background: linear-gradient(90deg, var(--signal-ice), rgba(103, 217, 255, 0.26));
    transform: translateY(-50%) scaleX(0);
    transform-origin: left center;
    animation: roadmap-line-build 2s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  .recommendation-loader span {
    position: relative;
    z-index: 1;
  }

  @keyframes roadmap-line-build {
    0%, 8% { transform: translateY(-50%) scaleX(0); opacity: 0.38; }
    82%, 100% { transform: translateY(-50%) scaleX(1); opacity: 1; }
  }

  @media (prefers-reduced-motion: reduce) {
    .recommendation-loader::after { animation: none; transform: translateY(-50%) scaleX(1); }
  }
  /* One continuous light field through intro, questions and recommendation. */
  :global(.site-color-field.site-color-field--persistent),
  :global(#main-content .roadmap[data-persistent-field]::before) {
    mask-image: linear-gradient(180deg, transparent, #000 var(--field-spread), #000 calc(100% - var(--field-spread)), transparent);
  }
  .roadmap { --cloud-white: #081f30; --mist: #081f30; --steel: #334e62; --signal-ice: #153f5a; }
  .roadmap .quiz-meta, .quiz-intro small, .quiz-intro p, .quiz-progress > span, .question-hint, .quiz-back,
  .consultation-support, .result-block > .text-mono, .result-block p,
  .result-block :global(li), .quiz-reset, .quiz-calculating p { color: #081f30; }
  .roadmap .section-kicker, .roadmap [data-service-link] { color: #081f30; }
  .progress-track { background: rgba(18, 53, 77, .2); }
  .answer-option { background: rgba(244, 249, 252, .65); color: #081f30; }
  .answer-option[aria-pressed='true'] { background: #143c58; color: #f5f9fc; }
  .answer-option:focus-visible, .quiz-back:focus-visible, .quiz-reset:focus-visible,
  .roadmap a:focus-visible, .roadmap .btn:focus-visible { outline: 2px solid #143c58; outline-offset: 4px; }
  .roadmap .btn { background: #0b2234; color: #f5f9fc; border-color: #0b2234; box-shadow: none; }
  .service-combination :global(span) { display: inline-block; padding: 7px 11px; border: 1px solid; border-radius: 999px; font-size: .8rem; line-height: 1.4; color: #081f30; border-color: rgba(18, 53, 77, .3); }
  .service-combination :global(span[data-priority='primary']) { background: rgba(240, 247, 251, .55); color: #081f30; border-color: #3a607a; }
  .quiz-reset { text-decoration-color: currentColor; }
  .recommendation-loader span { background: #163f59; box-shadow: none; }
  .recommendation-loader::before { background: rgba(18, 53, 77, .2); }
  @media (hover: hover) and (pointer: fine) { .answer-option:not([aria-pressed='true']):hover { background: #eaf3f8; color: #081f30; } .quiz-back:not(:disabled):hover { color: #081522; } }
</style>
