---
import { useTranslations } from '../../i18n/routes';

interface Props { locale: 'id' | 'en'; }
const { locale } = Astro.props;
const t = useTranslations(locale);
const content = locale === 'id'
  ? {
      title: 'Satu sistem untuk pertumbuhan digital Anda.',
      description: 'Website, SEO, dan social bergerak dengan pesan dan tujuan yang sama.',
      portfolio: 'Lihat Portofolio'
    }
  : {
      title: 'One system for your digital growth.',
      description: 'Website, SEO, and social move with the same message and goal.',
      portfolio: 'View Portfolio'
    };
---

<section class="hero" aria-labelledby="hero-title">
  <div class="hero-gradient" aria-hidden="true"></div>
  <div class="hero-scrim" aria-hidden="true"></div>

  <div class="hero-container container">
    <div class="hero-content">
      <h1 id="hero-title">{content.title}</h1>
      <p>{content.description}</p>
      <div class="hero-actions">
        <a href={`/${locale}/contact/`} class="btn btn-primary btn-cta">
          <span>{t('nav.start_project')}</span>
        </a>
        <a href={`/${locale}/work/`} class="btn btn-secondary">
          <span>{content.portfolio}</span>
        </a>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: max(700px, 100svh);
    display: grid;
    place-items: center;
    overflow: hidden;
    isolation: isolate;
    background: #070b12;
  }

  .hero-gradient,
  .hero-scrim { position: absolute; inset: 0; }

  .hero-gradient {
    z-index: 0;
    inset: -2%;
    background: url('/assets/hero-horizon.webp') center center / cover no-repeat;
    transform: scale(1.015);
    will-change: transform;
    animation: hero-art-drift 24s cubic-bezier(0.45, 0, 0.2, 1) infinite alternate;
  }

  .hero-scrim {
    z-index: 1;
    background: linear-gradient(180deg, transparent 0%, transparent 58%, rgba(7, 11, 18, 0.14) 76%, #070b12 100%);
  }

  .hero-container { position: relative; z-index: 2; padding-top: 132px; padding-bottom: 92px; }
  .hero-content { width: min(900px, 100%); margin-inline: auto; text-align: center; animation: hero-enter 700ms var(--ease-out) both; }

  .hero h1 {
    max-width: 13ch;
    margin: 0 auto 26px;
    font-size: clamp(3rem, 1.9rem + 4.35vw, 5.25rem);
    font-weight: 560;
    letter-spacing: -0.04em;
    line-height: 0.94;
    text-wrap: balance;
  }

  .hero-content > p {
    max-width: 55ch;
    margin: 0 auto 34px;
    color: rgba(226, 234, 243, 0.86);
    font-size: clamp(1rem, 0.92rem + 0.32vw, 1.18rem);
    line-height: 1.56;
  }

  .hero-actions { display: flex; flex-wrap: wrap; justify-content: center; gap: 12px; }

  @keyframes hero-enter {
    from { opacity: 0; transform: translate3d(0, 16px, 0); }
    to { opacity: 1; transform: translate3d(0, 0, 0); }
  }

  @keyframes hero-art-drift {
    from { transform: translate3d(-0.4%, 0.5%, 0) scale(1.015); }
    to { transform: translate3d(0.8%, -0.5%, 0) scale(1.045); }
  }

  @media (max-width: 760px) {
    .hero { min-height: max(680px, 100svh); }
    .hero-gradient { inset: -4%; background-position: 61% center; }
    .hero-container { padding-top: 122px; padding-bottom: 72px; }
    .hero h1 { max-width: 10ch; font-size: clamp(2.55rem, 10.5vw, 3.5rem); }
    .hero-content > p { max-width: 32ch; }
    .hero-actions { width: min(100%, 270px); margin-inline: auto; flex-direction: column; }
    .hero-actions .btn { width: 100%; }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-content,
    .hero-gradient { animation: none; }
    .hero-gradient { transform: scale(1.02); }
  }
</style>